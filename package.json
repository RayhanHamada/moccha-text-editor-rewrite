{
  "name": "mter",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "eslint": "^8.56.0",
    "turbo": "^1.11.3"
  }
}
